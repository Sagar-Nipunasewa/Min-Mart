import { tv } from "tailwind-variants";

const formInputItemVariants = tv({
  base: " mb-[14px] ",
});

const inputFieldVariants = tv({
  base: "border border-gray-300 p-[7.5px] w-[360px]  text-base ",
});

export { formInputItemVariants, inputFieldVariants };
